from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Builld ml project that predicts food delivery time in minutes.',
    author='saurav',
    license='',
)
